{
  "name": "frotahub-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "FrotaHub — interface web. Compilada pela automação e publicada no HostGator.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  }
}
